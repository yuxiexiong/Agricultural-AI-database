# Schema Layer

本目录保存字段、标签和质量规则，供人类维护者和自动化脚本统一口径。

## 当前文件

| 文件 | 内容 |
|---|---|
| `tags.yml` | 资源类型、来源类型、技术标签、应用场景、可信度等级 |
| `fields.yml` | 常用字段定义和必填规则 |
| `quality.yml` | 质量等级、证据等级、复核周期和引用规则 |

## 维护规则

- 新增标签前，先检查 `tags.yml` 是否已有同义标签。
- 新增字段前，先判断它是否能被多个文件复用。
- 字段定义应稳定，避免频繁改名导致自动化脚本失效。
